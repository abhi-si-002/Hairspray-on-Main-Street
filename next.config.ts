import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['lh3.googleusercontent.com','images.fresha.com','encrypted-tbn0.gstatic.com','static.wixstatic.com'],
  },
};

export default nextConfig;
